version: '3.7'

services:
  debug:
    build:
      context: .
      dockerfile: debug.Dockerfile
    command: -workspaceName "dev" -planPath plan.out
    volumes:
      - "/Users/dohyun/whatvita/rover/plan:/src"
    ports:
      - "9000:9000"
      - "2345:2345"